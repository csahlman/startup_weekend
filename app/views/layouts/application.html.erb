<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="container">
      <% if flash %>
        <% flash.each do |key, value| %>
          <div class="alert alert-danger"><%= raw value %><a href="#" class="close">&times;</a></div>
        <% end %>
      <% end %>
      <%= yield %>
    </div>

  </body>
</html>
